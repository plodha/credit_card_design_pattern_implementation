
import java.util.Date;

public class VisaCC extends CreditCard {

	public VisaCC(String cardNumber, Date expDate, String name, String cardType, boolean isValid) {
		super(cardNumber, expDate, name, cardType, isValid);
	}

}
